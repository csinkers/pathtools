customCommands:
  - key: '<f1>'
    context: 'files'
    command: "git difftool --dir-diff --no-symlinks"

  - key: '<f5>'
    command: 'git fetch --prune'
    context: 'localBranches'
    stream: true
